from lasso_improve_counting import *

# epsilon = float(input("请输入扰动的参数: "))  # 输入扰动的参数 epsilon
# p = float(input("请输入p: "))  # 输入p
# q = float(input("请输入q: "))  # 输入q
epsilon = 5
p = 0.5
q = 0.75
improved_counting(epsilon, p, q)
